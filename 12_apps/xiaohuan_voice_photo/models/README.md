# Models

Model binaries are intentionally not archived in git.

Place the Vosk Chinese model here before running the default listener:

```text
models/vosk-model-small-cn-0.22/
```

The original test machine used `vosk-model-small-cn-0.22`.
