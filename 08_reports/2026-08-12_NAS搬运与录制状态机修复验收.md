# 2026-08-12 NAS 搬运与录制状态机修复验收

## 现场基线

- 下游报告 session：`1786436322738206`。
- 旧首片 CSV 在约 16:23:40--16:25:25 存在多路共同断帧，主要 RGB 缺口约 98--99 秒。
- 修复前现场积压：约 21 段 / 33.37 GB，最老约 74.6 分钟。
- receiver 仍显示 `recording_all=true`，但六路已经 `record_accepting=false` 且触发低空间保护。

## 修复和部署

1. receiver 增加录制故障状态、开始前容量护栏和存储故障全局停录。
2. 取消存储恢复后的原 session 自动续录。
3. 关闭低复用率的多路增量镜像。
4. 大媒体文件改为 `dd iflag=direct` 搬运，录制时 1 worker、空闲时 2 worker，不支持时回退缓冲复制。
5. 已部署 receiver `build_source_hash=ee243f51569be7f3`，uploader 已重启并保留全部待搬数据。

## 自动化验证

- receiver/uploader 测试：9/9 通过。
- 新增容量不足拒绝开始、故障状态字段、禁用增量镜像不读 NAS 状态、direct-copy 数据一致性测试。
- Python 语法检查和 `git diff --check` 通过。

## 六路实机短录制

- session：`1786521576467325`。
- 实际时长：约 125 秒。
- 6/6 RGB MP4 与 6/6 Depth MKV 均可被 `ffprobe` 读取。
- RGB：`29.988--30.022 FPS`。
- Depth：`30.022--30.068 FPS`。
- 最大帧间隔：`60.23 ms`。
- 超过 200 ms 的间隔：0。
- 时间戳倒退：0。
- 录制期间 `record_queue_total_bytes=0`，六路 `record_accepting=true`。
- NAS 搬运观测约 `147--151 Mbps`，高于六路约 85 Mbps 产出。
- 停止后 receiver finalizer 和录制写队列均为 0。

## 当前状态

测试结束后 receiver 为 `recording_state=idle`，六路媒体仍在线。历史 backlog 仍在后台清理，因此 `recording_delivery_pending=true` 是预期状态；不得在 ready marker 出现前把 NAS 目录当成已完整交付。
