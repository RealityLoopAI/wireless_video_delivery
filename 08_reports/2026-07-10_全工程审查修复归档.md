# 2026-07-10 全工程审查修复归档

## 范围

审查并整改 sender、receiver、CLOCK_SYNC、录制、Web、脚本、配置和构建测试。改动保持现有相机 SDK、GStreamer 编码、主媒体协议和 RGBD 下游格式，不做协议换代。

## 主要结论

1. 过去的掉线、Web admin timeout、MP4 收尾失败并非单点问题，而是队列无统一字节上限、线程/fd 生命周期、全局锁重处理和异常收尾共同造成。
2. 时间轴异常既有 SDK system timestamp 非单调问题，也有旧 offset 长期有效、模型漂移和错误地把设备 uptime 映射成系统 epoch 的风险。
3. 接收端对 UDP/压缩包缺少严格资源边界，局域网异常包可造成大内存分配和线程膨胀。
4. 现场设备源码和运行二进制长期存在 commit 不一致，必须把 build 版本上报作为验收项。

## 实施摘要

完整技术说明见：

[../04_docs/44_全链路可靠性安全与性能整改_2026-07-10.md](../04_docs/44_全链路可靠性安全与性能整改_2026-07-10.md)

本轮新增自动验证：

1. 所有 sender 配置模板严格解析。
2. receiver 端口冲突和优雅退出。
3. 非法身份、超长字段、恶意 UDP assembly、压缩 chunk 越界/重叠。
4. CLOCK_SYNC probe/report 和模型状态。
5. Web 未授权/授权访问。
6. 录制队列排空、Depth MKV 收尾、RGB MP4 可读和 seek、CSV 新字段。
7. ASan/UBSan 构建和静态分析。

## 部署要求

1. 不覆盖各设备现场相机配置、Wi-Fi 守护参数和私有备份。
2. 各架构本机编译，不复制二进制。
3. receiver 无活动录制且 finalizing/queue 均为零后再重启。
4. 部署后检查 `build_commit`、`build_dirty`、`build_source_hash`、每路 FPS/码率、CLOCK_SYNC offset/delay、队列和错误计数。
5. 做一次短录制并用 `ffprobe`、CSV 行数和最终状态验收。

## 部署前实机验证

1. `192.168.66.196` 在独立 `/tmp` 构建 receiver，通过带真实 `ffmpeg/ffprobe` 的集成测试：RGB MP4 可读取并可 seek 到 1 秒位置，Depth FFV1 MKV 可读，CSV schema 完整，进程可优雅退出。
2. `192.168.66.206` 和 `192.168.66.133` 均在独立 `/tmp` 使用各自 ARM64 SDK 本机编译 sender，并通过全部配置模板验证。
3. 测试阶段未替换在线服务、未改现场相机配置；确认 receiver 当时没有活动录制后才进入正式部署阶段。

## 部署后实机验收

1. receiver 和三个在线 sender 使用同一份源码构建，关键帧修复后最终源码标识为 `build_source_hash=c47ab0733a437d27`；不覆盖设备专用相机和 Wi-Fi 配置。
2. 三路 RGB/Depth 采集和发送稳定在约 30 FPS，实测窗口内坏 JPEG、发送失败和编码滞后重置均为 0。
3. 三个 CLOCK_SYNC 模型均有效，局域网实测 offset 约为 `-0.7~-1.1 ms`，delay 约为 `1.8~2.5 ms`。
4. 三路 12 秒录制都生成可读、可 seek 的 RGB MP4 和 Depth FFV1 MKV；CSV 的 `global_timestamp_us` 按流严格单调，`clock_sync_valid` 覆盖率 100%，无 `.part/.tmp` 残留。
5. 发现旧实现把 downstream force-key-unit 直接从 encoder 发出，事件实际绕过了编码器，录制开始最长等待下一个 IDR。进一步发现 Rockchip BSP 插件不会正确清理 `GST_CLOCK_TIME_NONE` 强制帧的 pending 状态，使后续 ASAP 请求被 GStreamer 当作重复事件忽略。最终从 appsink 发送带下一输入帧明确 running-time 的 upstream 事件：纯编码器测试 3/3 次均在当前 encode call 生成 IDR，实机连续 5 次请求延迟为 `38.5~58.9 ms`。
6. 复测时 NAS/CIFS 多次出现约 20~30 秒的写入停顿，三路并行录制的 receiver 队列峰值 15.2 MB，存储恢复后全部排空且文件完整。这证明可靠录制队列能隔离短时 NAS 抖动；如果停顿持续到超过配置的 2 GB 全局上限，仍需修复 NAS/网络或改为本地缓存后异步迁移。
7. 接收端启停脚本改为按通用 `gemini_receiver .*--config` 命令行识别残留进程，可清理历史上以相对路径启动的 receiver，避免新服务因端口冲突循环重启。
8. 为符合“录制数据完整稳定优先”，本机主 RGB/Depth 从 UDP 分片改为独立 TCP 连接。切换前预览/IDR 突发可观察到 UDP assembly 超时；切换后读取 64 KiB Web H.264 的复测中，UDP completed/expired/invalid 增量全为 0，TCP Send-Q 回落为 0。旧 UDP 配置已保留在桌面备份。

## 未解决的物理边界

软件时间轴不等于曝光硬同步。无同步线的自由运行相机仍可能存在一帧范围内的内容相位差，后续数据集应继续使用 `global_timestamp_us + 内容标定补偿`，并定期复核补偿漂移。
