# 录制任务音频与质量门禁部署记录

## 1. 代码基线

- 开发分支：`codex/streaming-nas-spool`
- 当前验证提交：`3592fdb`
- sender 运行改动起始提交：`343b909`
- 本地工作树在归档时无未提交代码。

## 2. 本轮修复

1. sender 在分片边界持续重试强制 IDR，直到真正观察到编码 IDR。
2. receiver 严格判定 RGB/Depth 覆盖率、首尾延迟、最大缺口和媒体时长差。
3. NAS uploader 保留 receiver 生成的全部质量证据，不再丢失有效帧数和时长差。
4. 任务音频直接复用 Opus RTP，按视频任务和 15 分钟边界写入每个相机目录。
5. 零真实 RTP 只发布 `no_input` 证据，不再生成伪造的全静音 `audio.opus`。
6. 3 秒无音频包告警，5 秒无包通知 sender 重建 ALSA/GStreamer 采集。控制目标来自 receiver 动态观测的 sender 源 IP，不依赖 DHCP 固定地址。

## 3. 测试结果

- ARM 开发机：`ctest` 14/14 通过，sender/receiver 均编译通过。
- x86 receiver 机：direct-NAS 和 staging/uploader 真实 `ffmpeg` 流程通过。
- x86 完整强化测试与生产录制并跑时，主预览刷新因硬解/CPU 争用超时；该项待生产录制空闲后单独复测。
- 历史音频审计干跑：扫描 1098 段，发现 659 段为零真实 RTP；归档文件已生成，尚未在录制期间批量改写历史元数据。

## 4. 实机部署状态

2026-08-25 生产接收端已在录制空闲窗口完成切换：

- receiver：`3592fdb`
- 音频归档程序、任务音频配置、NAS uploader 和 Web 静态页：与当前分支同步
- 服务重启后六路媒体连接恢复，接收队列和待收尾队列均为 0

已部署 sender `343b909` 并恢复约 30 FPS：

- `rk3588-ubuntu`
- `lubancat-52d2ef0c`
- `lubancat-e8cc0cb3`
- `orangepi5pro-b439137c`
- `orangepi5pro-d12a4719`
- `orangepi5pro-f022c4`

三台语音设备已部署音频双发和重建控制：

- `lubancat-52d2ef0c`
- `lubancat-e8cc0cb3`
- `orangepi5pro-d12a4719`

部署时发现三台语音设备的现场 `zz-rtp-stream.conf` 仍把实时流和归档流设为关闭。第一次短测正确生成了 `no_input` 证据且没有伪造全静音 `audio.opus`。同步仓库配置并重启后，receiver 观测三路音频最近包年龄约 22-32 ms，包计数持续增长，无无效 RTP 包。

## 5. 生产短测结果

全量升级后执行约 14 秒六路真实录制：

- 六路 `recording_quality_status=complete`。
- 停止接口约 11 ms 返回，后台六路收尾约 1 秒完成。
- 六路 RGB/Depth 有效帧约 413-420 帧，媒体时长差最大约 19.6 ms。
- `lubancat-52d2ef0c` 音频 701/701，最长缺口 0 ms，`complete`。
- `lubancat-e8cc0cb3` 音频 701/701，最长缺口 0 ms，`complete`。
- `orangepi5pro-d12a4719` 音频 697/701，最长缺口 40 ms，`complete`。
- 其余三路没有配置麦克风，明确写为 `no_input`，不生成伪音频文件。

## 6. 仍需完成

1. 执行历史零输入元数据 `--apply`；录制期间不得运行批量改写。
2. 做完整 15 分钟切片复测。
3. 做后续 5 小时异步复测，并使用外部严格媒体报告验收。

生产 receiver 的独立 release 二进制保留在 `/home/fz/gwv3-release-3592fdb/12_build/bin/gemini_receiver`，旧版本备份保留在 `/home/fz/gwv3-backup-20260825/`。
