# Runtime Logs

该目录仅作为现有服务的运行日志根目录。实际日志、轮转文件和现场诊断输出由 `.gitignore` 排除，不应提交。

长期有效结论写入 `04_docs/` 对应主题；一次性测试证据保留在自动测试输出、issue 或 commit/PR 说明中，不再新增按日期命名的 Markdown 报告。

不要把这里当作唯一监控或审计系统。日志会轮转，服务状态、NAS 完成标记和录制元数据分别承担不同职责；问题复现时应同时保存运行版本、时间范围和必要日志片段。
