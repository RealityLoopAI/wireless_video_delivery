# Samples

本目录只用于小型、脱敏、可重复使用的配置或协议样例。当前没有提交媒体样例。

允许提交：

- 不含真实设备身份和凭据的短 JSON/CSV 示例。
- 能覆盖协议边界或测试夹具的最小二进制样例。
- 明确标注来源、格式和生成方法的数据。

禁止提交：

- 真实 RGB、Depth、音频或 NAS 录制目录。
- 人脸、声音、位置等个人或现场敏感数据。
- 密码、token、Wi-Fi PSK、SSH key 或私有地址清单。
- 可由测试在运行时生成的大文件。

新增样例时应同时添加消费它的测试，并保持仓库体积可控。
