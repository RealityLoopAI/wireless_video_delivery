# 发送端交付与接收端对接说明 v3

## 1. 交付状态

本交付包包含香橙派 5 Pro 发送端首版实现和接收端对接所需协议说明。

发送端身份：

```text
sender_id = orangepi5pro-01
camera_id = cam01
camera_serial_number = AY2M54302ZH
```

发送端硬件与相机实测结果：

```text
发送端设备: Orange Pi 5 Pro / RK3588S / aarch64
系统: Orange Pi Ubuntu 22.04 Jammy
相机型号: SV1301S_U3
连接: USB3.0
RGB USB: 2bc5:0511
Depth USB: 2bc5:0614
SDK: Orbbec SDK v1.10.27
当前 RGB profile: 1920x1080@30 MJPG
当前 RGB encoding: H.264 / mpph264enc / 12000000 bps
当前 Depth profile: 640x400@30 raw depth
当前 Depth compression: zlib
depth_scale: 1
```

当前发送端已经完成：

1. Orbbec SDK 真机打开。
2. RGB 与 Depth 同时采集。
3. RGB 经 Rockchip `mpph264enc` 硬件编码为 H.264。
4. Depth 以 `uint16` raw frame 采集，当前默认配置使用 `zlib` 无损压缩发送。
5. 状态 JSON 消息发送。
6. 媒体帧二进制封包。
7. 本机日志和一键启动脚本。

2026-05-11 调试修复：

1. Rockchip `mpph264enc` 在本机实测中不能直接稳定接收 BGR 路径，原管线只输出 AUD/SPS/PPS 头信息，不输出有效图像帧。
2. 发送端已在 `videoconvert` 后强制转换为 `NV12` 再进入 `mpph264enc`。
3. 本机回环抓包验证：RGB H.264 payload 可被 `ffprobe` 识别为有效 H.264 码流。
4. 发送端现在会根据 H.264 NAL type 5 设置媒体帧头 `flags.bit0 = key_frame`。

2026-05-11 Depth 压缩验证：

1. 发送端已支持 `depth_transport.compression = zlib`，接收端需要先按 zlib 解压后再按 `uint16 little-endian` 解析。
2. 默认运行配置为 `06_configs/sender_orangepi5pro-01_depth_zlib.json`。
3. 兼容 raw Depth 配置为 `06_configs/sender_orangepi5pro-01.json`，用于未升级 zlib 解压能力的接收端。
4. 本机回环实测：`640x400` Depth 从 512000 字节/帧压到约 81KB/帧，平均约为原始大小的 15.9%。

## 2. 当前端口约定

接收端需要监听：

```text
状态通道: UDP 50011
媒体通道: TCP 50010
```

配置文件位置：

```text
06_configs/sender_orangepi5pro-01_depth_zlib.json
```

发送端会主动连接接收端：

```text
receiver.ip = 192.168.1.107
receiver.media_port = 50010
receiver.status_port = 50011
```

## 3. 状态通道

状态通道使用 UDP。

每个 UDP datagram 是一条 UTF-8 JSON 消息，发送端当前会在 JSON 后追加一个换行符 `\n`。接收端解析时可以先 trim 空白字符。

消息类型遵循：

```text
sender_hello
camera_announce
heartbeat
camera_offline
event
```

字段语义以 `04_docs/03_中间传输数据格式_v3.md` 为准。

当前实现中：

1. 启动后发送 `sender_hello`。
2. RGB 和 Depth 都拿到帧后发送 `camera_announce`。
3. 默认每 1000 ms 发送一次 `heartbeat`。
4. 编码器失败、网络失败等错误写入日志，并通过 `event` 上报。

## 4. 媒体通道

媒体通道使用 TCP。TCP 流中连续发送多个媒体 packet。

每个 packet 结构：

```text
固定头 94 字节
sender_id bytes
camera_id bytes
codec_or_compression bytes
payload bytes
```

接收端需要循环读取：

1. 先读取 94 字节固定头。
2. 校验 magic、header_version、header_size。
3. 根据 `sender_id_len`、`camera_id_len`、`codec_len` 读取 3 段字符串。
4. 根据 `payload_size` 读取 payload。
5. 处理完一个 packet 后继续读下一个 packet。

## 5. 固定头格式

固定头使用小端序。

```text
offset  size  type    field
0       4     uint32  magic
4       2     uint16  header_version
6       2     uint16  header_size
8       1     uint8   stream_type
9       1     uint8   reserved0
10      4     uint32  flags
14      2     uint16  sender_id_len
16      2     uint16  camera_id_len
18      2     uint16  codec_len
20      8     uint64  frame_id
28      8     uint64  timestamp_us
36      8     uint64  system_timestamp_us
44      8     uint64  pair_id
52      4     uint32  width
56      4     uint32  height
60      2     uint16  pixel_format
62      8     uint64  payload_size
70      8     uint64  uncompressed_size
78      16    bytes   reserved
```

固定值：

```text
magic = 0x33565747
magic bytes = 47 57 56 33
magic ascii = "GWV3"
header_version = 1
header_size = 94
```

## 6. 枚举值

`stream_type`：

```text
1 = rgb
2 = depth_raw
```

`pixel_format`：

```text
1 = encoded_video
2 = depth_u16
```

`flags`：

```text
bit 0 = key_frame
bit 1 = dropped_before
bit 2 = end_of_segment_hint
bit 3 = has_system_timestamp
```

当前实现会设置 `has_system_timestamp`。

## 7. RGB Payload

RGB packet：

```text
stream_type = 1
pixel_format = 1
codec_or_compression = h264
payload = H.264 byte-stream / AU
```

发送端当前编码路线：

```text
Orbbec RGB MJPG -> OpenCV decode BGR -> GStreamer mpph264enc -> h264parse -> TCP media packet
```

接收端建议：

1. 按 H.264 Annex B byte-stream 处理。
2. 录制时优先直接封装到 MP4/MKV，不要重编码。
3. 预览时再解码。

## 8. Depth Payload

Depth packet：

```text
stream_type = 2
pixel_format = 2
codec_or_compression = none 或 zlib
payload = uint16 depth_raw 一整帧，或 zlib 压缩后的 uint16 depth_raw
uncompressed_size = width * height * 2
```

当前实测规格：

```text
width = 640
height = 400
uncompressed_size = width * height * 2 = 512000 bytes
depth_scale = 1
```

当 `codec_or_compression = none` 时，`payload_size = uncompressed_size`。

当 `codec_or_compression = zlib` 时：

1. `payload_size` 是压缩后的字节数。
2. `uncompressed_size` 是解压后的原始深度字节数。
3. 接收端必须先用 zlib 解压到 `uncompressed_size`，再按 `uint16 little-endian` 解析。

`depth_scale` 由 `camera_announce.depth_profile.depth_scale` 提供。

## 9. 接收端最小实现建议

接收端首版可以先实现以下最小闭环：

1. UDP `50011` 打印并保存状态 JSON。
2. TCP `50010` accept 一个发送端连接。
3. 按 94 字节固定头解析媒体 packet。
4. 校验 `sender_id=orangepi5pro-01`、`camera_id=cam01`。
5. RGB payload 写入临时 `.h264` 文件，验证可被 FFmpeg 解码。
6. Depth payload 在 `codec_or_compression=zlib` 时先解压，再按帧写入临时 raw 文件，或写入 MKV/自定义无损容器。
7. 根据 `frame_id`、`timestamp_us` 记录逐帧索引。

## 10. 发送端运行方式

构建：

```bash
cmake -S . -B 12_build
cmake --build 12_build -j4
```

配置校验：

```bash
./12_build/bin/gemini_sender --config 06_configs/sender_orangepi5pro-01_depth_zlib.json --validate-config
```

本机采集验证，不发送、不预览：

```bash
./12_build/bin/gemini_sender --config 06_configs/sender_orangepi5pro-01_depth_zlib.json --run-seconds 5 --no-preview --no-send
```

后台启动：

```bash
./05_tools/start_sender.sh
```

所有启动脚本都会先执行 `05_tools/sender_preflight.sh`，在终端显示发送端 ID、相机 ID、接收端 IP/端口、RGB/Depth profile、编码器、Depth 压缩、预览配置、日志目录、路由和 Wi-Fi 链路信息。预检失败时输出中文错误并停止启动。

状态查看：

```bash
./05_tools/status_sender.sh
```

停止：

```bash
./05_tools/stop_sender.sh
```

## 11. 已知限制

1. 传输协议是当前首版约定：状态 UDP、媒体 TCP。文档原需求没有写死协议，后续可替换为 SRT/RTP 等。
2. 当前实现的 Depth 压缩方式为 `none` 和 `zlib`，尚未实现 LZ4/Zstd。
3. 当前 RGB 输入使用相机 MJPG，再硬编成 H.264；1080p@30 已在本机启动验证。
4. 当前只按一台相机 `cam01` 配置验证。
5. 接收端未启动时，发送端会继续采集，但媒体 TCP 会报 `Connection refused` 并周期重试。
6. Orbbec SDK 停流时会输出若干 USB timeout warning，当前观察为 SDK 退出阶段日志，不影响本次采集验证结果。
