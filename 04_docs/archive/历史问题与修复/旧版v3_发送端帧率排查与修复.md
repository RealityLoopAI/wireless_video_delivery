# 发送端帧率排查与修复说明 v3

## 1. 问题现象

2026-05-26 现场检查时，发送端 `orangepi5pro-b439137c / cam02` 能正常连接接收端，但接收端统计显示实际到达帧率不足 30fps：

```text
RGB:   约 17.4 fps，约 7 Mbps
Depth: 约 17.4 fps，约 8 Mbps
```

同时发送端日志中持续出现：

```text
Pipeline source frameset queue fulled, drop the oldest frame
```

该日志说明发送端主循环消费 Orbbec frameset 的速度低于相机输出速度，SDK 队列满后开始丢旧帧。

## 2. 排查结论

网络不是主要瓶颈：

```text
Wi-Fi: 5G，协商 433 Mbps
Ping 接收端: 0% packet loss，平均约 2ms
TCP 发送队列: 基本为空
```

本机关闭本地预览后帧率仍只有约 18fps，因此预览窗口不是主因。

根因在发送端 H.264 编码取输出逻辑。每帧送入 GStreamer 后，代码使用：

```cpp
gst_app_sink_try_pull_sample(..., 20 * GST_MSECOND)
```

循环读取编码输出。最后一次读取通常会等到 20ms 超时才退出，导致每帧额外引入固定等待，主循环无法稳定消费 30fps frameset。

## 3. 修复方式

将编码输出 drain 改成非阻塞读取：

```cpp
gst_app_sink_try_pull_sample(..., 0)
```

这样每次只取当前已经编码完成的 sample，不再为“没有更多 sample”额外等待 20ms。GStreamer pipeline 仍然保留 appsink 缓冲和 drop 策略，后续帧到来时会继续取出已经完成的编码输出。

修复文件：

```text
01_sender_linux/src/gst_h264_encoder.cpp
```

## 4. 验证结果

修复后重新编译并重启发送端，接收端 10 秒采样结果：

无本地预览：

```text
RGB:   约 30.08 fps，约 12.02 Mbps
Depth: 约 30.18 fps，约 14.27 Mbps
```

带本地预览：

```text
RGB:   约 29.28 fps，约 11.69 Mbps
Depth: 约 29.48 fps，约 13.93 Mbps
```

修复后发送端恢复接近 30fps。Orbbec 队列满日志从持续高频刷屏下降为零星出现。

## 5. 后续注意

如果再次出现接收端帧率明显低于 30fps，应按以下顺序检查：

1. 接收端 `/api/status` 中对应相机的 `rgb_packets` / `depth_packets` 10 秒差分。
2. 发送端 `sender_stdout.log` 是否持续出现 frameset queue full。
3. `ss -tin` 中 TCP 发送队列是否长期堆积。
4. Wi-Fi 信号、ping 丢包和发送端 CPU 温度。
5. 是否重新引入阻塞式等待、同步磁盘写入或高成本预览处理。

## 6. 2026-05-28 RK3588 媒体 TCP 背压掉线

### 6.1 现象

RK3588 发送端单路相机运行时，本地采集仍然正常，日志中仍有 `rgb_input_fps` / `depth_input_fps`，但接收端帧计数不再增长，发送端统计变成：

```text
rgb_sent_packets_s=0
depth_sent_fps=0
rgb_send_failures>0
depth_send_failures>0
```

此时 ping 接收端正常，接收端 TCP 端口也可连接，说明不是相机采集掉线，也不是接收端 IP 错误。

### 6.2 原因

发送端 media TCP socket 遇到发送背压。现场系统默认：

```text
net.core.wmem_max = 212992
net.core.wmem_default = 212992
```

虽然配置请求 1 MiB 发送缓冲，但实际受 `wmem_max` 限制，只能得到约 213 KiB。1080p H.264 和 Depth 包连续积压时，几个媒体包就可能填满发送队列。旧逻辑会持续丢包计数，但不会因为连续背压主动重连，所以表现为采集还在跑、媒体发送长期为 0。

### 6.3 修复

代码修复：

```text
01_sender_linux/src/transport.cpp
01_sender_linux/include/gwv3_sender/transport.hpp
01_sender_linux/src/main.cpp
```

修复点：

```text
连续 TCP 背压丢包后主动关闭 media socket 并重连。
发送失败时打印 media send failed，日志包含 camera_id、stream 和具体错误。
发送端配置 send_buffer_bytes 提升到 4194304。
```

系统参数：

```bash
sudo sysctl -w net.core.wmem_max=8388608 net.core.wmem_default=1048576
```

持久化：

```text
/etc/sysctl.d/99-gemini-video-transport.conf
```

内容：

```text
net.core.wmem_max = 8388608
net.core.wmem_default = 1048576
```

### 6.4 验证

修复后单路 `cam01` 20 秒接收端差分：

```text
RGB:   +400 packets，约 20 fps
Depth: +602 frames，约 30 fps
TCP:   ESTABLISHED，Send-Q=0
```

发送端近期日志无 `media send failed`，`rgb_send_failures=0`，`depth_send_failures=0`。双路发送时仍需关注总带宽、接收端解码压力和 Wi-Fi 链路，但同类“背压后一直不恢复”的问题应由发送端自动重连恢复，并在日志中留下明确原因。

## 7. 2026-05-28 RK3588 双路满规格 Depth 停滞

### 7.1 现象

双相机满规格运行时，`cam02` 的 RGB/Depth 可接近 30fps，但 `cam01` 经常只有 RGB 接近 30fps，Depth 为 0fps。Orbbec SDK 同时可能刷出：

```text
usb_request_cancel returned error, endpoint:129, error:无法分配内存, number:12
Failed to re-submit asynch I/O transfer
```

如果旧发送端子进程没有退出干净，下一次启动还可能报：

```text
uvc_open ... already opened
```

### 7.2 原因

当前系统默认 `usbfs_memory_mb=16`，双路 1080p RGB + 640x400 Depth 会消耗更多 libusb 传输缓冲；当内核 USB 缓冲不足时，SDK 可能只持续输出某一路 RGB，Depth 长时间不出帧。单独 probe 同一台相机能跑到 30fps，不代表双路满规格也能在 16MB usbfs 缓冲下稳定运行。

同时如果 `net.core.wmem_max=212992`，配置里的 4MiB TCP send buffer 实际不会生效，满规格双路发送会出现 `media TCP packet dropped under backpressure`。

### 7.3 修复

发送端改为双层自恢复：

```text
某 camera_id 的 RGB 或 Depth 超过默认 8 秒无帧，但 worker 仍在返回 frameset：只重启该相机 pipeline。
worker 卡死，waitForFrames 不返回：发送端子进程退出，由 sender_watchdog.sh 重新拉起。
同一 camera_id 的采集停滞重连默认有 30 秒冷却，避免系统 USB 缓冲不足时反复冲击 SDK。
stop_sender.sh 会清理残留 gemini_sender，普通 kill 超时后强杀，避免 UVC 设备被旧进程占用。
```

启动预检会打印并警告 `usbfs_memory_mb` 和 `net.core.wmem_max`。满规格双路建议先以 root 设置：

```bash
echo 256 | sudo tee /sys/module/usbcore/parameters/usbfs_memory_mb
echo 8388608 | sudo tee /proc/sys/net/core/wmem_max
echo 1048576 | sudo tee /proc/sys/net/core/wmem_default
```

### 7.4 验证重点

```text
sender.log 中应能看到 camera disconnected ... capture stream stalled 后只重连对应 camera_id。
pgrep 中不应残留多个 gemini_sender 子进程。
ss -tinpe dst <receiver>:50010 的 Send-Q 不应长期堆积。
预检输出的 usbfs_memory_mb 应为 256 或更高，wmem_max 应不低于 4194304。
```

## 8. 2026-06-05 最新分支现场复测

### 8.1 现象

最新分支启动 `06_configs/sender_orangepi5pro-01_depth_zlib.json` 后，发送端采集侧正常：

```text
rgb_input_fps ~= 30
depth_input_fps ~= 30
rgb_encode_avg_ms < 1
depth_compress_avg_ms ~= 5
```

但媒体发送侧出现 `media TCP packet dropped under backpressure`，`ss -tinpe 'dst 192.168.66.196:50010'` 的 Send-Q 长期堆积。

### 8.2 根因

本次现场同时命中两个问题：

1. `sender_orangepi5pro-01*.json` 中的旧 serial `AY2M54302ZH` 已不能匹配当前 Orbbec 设备；SDK 枚举到的 depth serial 为空，但 USB 配对 RGB serial 为 `AY2T7410103`。旧配置会导致配置相机启动失败，热插拔扫描再把同一台设备按动态相机拉起，表现为接收端出现额外相机 key。
2. 发送端本机只有一个 media TCP 连接时，接收端 `http://192.168.66.196:8080/api/status` 仍显示多路 active media client，包括 `orangepi5pro-66-133_cam02` 和 `rk3588-ubuntu_*`。这说明接收端还在被其他发送端/残留发送端持续灌流，导致当前单路也被 TCP 背压拖慢。
3. 临时按 IP 固定成 `orangepi5pro-66-206_cam01` 会改变接收端相机 key；如果 Web 预览页仍选中旧 key 或主预览仍指向其他发送端，就会表现为“发送端没发”，实际 `/api/status` 里当前 key 的 `rgb_packets/depth_packets` 仍在增长。

### 8.3 修复与处理

1. 将 `sender_orangepi5pro-01.json` 和 `sender_orangepi5pro-01_depth_zlib.json` 的 `serial_number` 更新为当前可匹配的 `AY2T7410103`。
2. 现场单路配置恢复之前可用的 `sender_id=auto` 派生方式；本机 wlan0 MAC `9c:b8:b4:39:13:7c` 会派生为 `orangepi5pro-b439137c`。相机 key 保持之前的 `cam02`，避免接收端预览和录制控制因为 key 改名看不到当前流。
3. 增加 `hotplug.enabled` 配置。默认保持热插拔开启以兼容 RK3588 多相机交付配置，但 `sender_orangepi5pro-01*.json` 明确设置为 `false`，确保预期一路时不会自动拉起额外相机 key。
4. 发送端 TCP 预写入丢包判断只在内核发送队列已经达到可用容量时触发；如果只是当前包不能一次性放入 socket 队列，则交给 `send_timeout_ms` 等待 TCP 排水，避免默认较小内核缓冲下过早整包丢弃。
5. Web 预览需要选中恢复后的 key；必要时调用 `POST /api/preview/main-target?sender_id=orangepi5pro-b439137c&camera_id=cam02` 将主预览切回本机单路。
6. 运行规格跟随当前默认交付档，Orange Pi 单路 Depth 调整为 `320x200@30 y12`；身份字段、相机槽位、serial 绑定和热插拔策略保持现场适配，不随仓库默认 ID 改名。
7. 使用 root 设置现场运行参数：

```bash
sudo sysctl -w net.core.wmem_max=8388608 net.core.wmem_default=1048576
echo 256 | sudo tee /sys/module/usbcore/parameters/usbfs_memory_mb
echo -1 | sudo tee /sys/module/usbcore/parameters/autosuspend
```

### 8.4 剩余风险

如果接收端管理接口仍显示多路 `active_media_clients`，或者其他 sender 的 `media_age_ms` 持续刷新，发送端单路 30fps 采集正常也不能保证端到端 30fps。需要先停止其他发送端或重启接收端并确认只剩预期 media client，再以 Send-Q 和 `rgb_send_failures/depth_send_failures` 判断链路是否恢复。

本机单路配置启动后，预检中应看到：

```text
config_sender_id: auto
sender_id: orangepi5pro-b439137c
camera_count: 1
camera_id: cam02
hotplug: enabled=false
transport.send_buffer_bytes: 4194304
```
