# 当前上下文交接摘要

更新时间：2026-06-16 17:00 HKT

本文用于在更换控制设备、换人接手或上下文丢失后，快速恢复当前项目状态。本文不包含明文密码、私钥、token 等敏感信息。

## 当前目标

当前系统是多发送端 RGBD 无线采集与接收录制链路：

- 多台发送端采集 Orbbec RGBD 相机。
- 接收端 `192.168.66.196` 负责接收、网页预览、录制落盘。
- 当前重点是稳定多设备 30fps 发送、网页预览、录制完整性、时间同步，以及树莓派 Gemini305 适配。

## 当前在线设备与角色

| 角色 | IP | 主机名 | 用户 | 当前状态摘要 |
|---|---:|---|---|---|
| 接收端 / 录制端 | `192.168.66.196` | `fz-Standard-PC-i440FX-PIIX-1996` | `fz` | receiver 与 web monitor 运行中，Web 入口 `http://192.168.66.196:8080/` |
| 当前控制端 / 本机发送端 | `192.168.66.85` | `rk3588-ubuntu` | `linaro` | 本机 sender 运行中，当前只在线 `cam01`，`cam02` 因配置序列号未找到离线 |
| 树莓派发送端 | `192.168.66.210` | `berry-desktop` | `berry` | Gemini305 V4L2 RGBD，已整理到唯一正式树莓派分支 |
| OrangePi 发送端 | `192.168.66.206` | `orangepi5pro` | `orangepi` | 接收端显示 `orangepi5pro-b439137c_cam02` |
| OrangePi 发送端 | `192.168.66.133` | `orangepi5pro` | `orangepi` | 接收端显示 `orangepi5pro-c973b736_cam02` |

SSH 接入、工程路径和新控制设备迁移步骤见：

- `04_docs/23_SSH接入信息与控制设备迁移说明_2026-06-16.md`

## 重要仓库与分支状态

主仓库：

- GitHub：`RealityLoopAI/wireless_video_delivery`
- 本机主目录：`/home/linaro/桌面/wireless_video_delivery`
- 主分支：`main`
- 生成本文前，本机 `main` 最新提交：`8c2f5ed Document SSH handoff information`

树莓派正式分支已经整理为唯一远端分支：

- `origin/raspberrypi-latest-20260605-175527`
- 当前最新提交：`ad0bba5 Use default auto exposure for Raspberry Pi Gemini305`
- 已删除远端临时分支：`raspberrypi-latest-20260605-175527-local`

保留的本地备份：

- 本机：`backup/remote-raspberry-local-before-delete-20260616`
- 本机旧 scratch worktree：`/tmp/wvd-rpi-gemini305`
- 本机旧 scratch 分支：`backup/raspberry-local-scratch-20260616`
- 树莓派本机：`backup/raspberry-before-consolidate-20260616_165019`
- 树莓派本机 stash：`pre-consolidate-raspberry-config`

这些备份不是正式协作入口，确认一段时间没有问题后可以再清理。

## 当前本机未提交内容

截至本文生成时，本机主仓库仍有未提交改动。不要盲目 `git add -A`。

未提交的 sender 配置：

- `06_configs/sender_orangepi5pro-01.json`
- `06_configs/sender_orangepi5pro-01_depth_zlib.json`
- `06_configs/sender_rk3588-01_cam02_depth_max.json`
- `06_configs/sender_rk3588-01_cam02_test.json`
- `06_configs/sender_rk3588-01_one_camera.json`
- `06_configs/sender_rk3588-01_two_cameras.json`
- `06_configs/sender_rk3588-01_two_cameras_swapped_test.json`

未跟踪文档：

- `04_docs/22_全链路工程审查报告_2026-06-16.md`

这些文件是之前工作遗留状态；提交前需要逐个确认是否属于当前要推送的范围。

## 最近已完成并推送的关键事项

### 1. SSH 交接文档

已推送：

- `8c2f5ed Document SSH handoff information`
- 文档：`04_docs/23_SSH接入信息与控制设备迁移说明_2026-06-16.md`

该文档只记录非敏感 SSH 坐标，不含明文密码。

### 2. 树莓派 Gemini305 默认曝光配置

已推送到树莓派正式分支：

- `ad0bba5 Use default auto exposure for Raspberry Pi Gemini305`
- 分支：`raspberrypi-latest-20260605-175527`

树莓派当前运行配置：

```json
"color_controls": {
  "auto_exposure": true,
  "auto_exposure_priority": 0,
  "exposure": 156,
  "gain": 16,
  "power_line_frequency": 3
}
```

树莓派运行态已验证：

- `auto_exposure: Auto Mode`
- `exposure_time_absolute=156`
- `gain=16`
- `rgb_ae=1`
- RGB/Depth 约 30fps
- 最近验证时坏帧与发送失败为 0

### 3. 树莓派分支整理

已完成：

- 远端只保留 `raspberrypi-latest-20260605-175527`
- 树莓派本机 tracking 已改为 `origin/raspberrypi-latest-20260605-175527`
- 远端 `raspberrypi-latest-20260605-175527-local` 已删除
- 树莓派本机工作区干净

## 时间同步状态

已对当前在线设备执行过时间同步复核和 `chronyc makestep`。

当前关系：

- 接收端 `192.168.66.196` 同步公网 NTP。
- 本机、树莓派、两个 OrangePi 都同步到接收端。
- 最近复核时，各设备偏差在微秒到亚毫秒级。

后续如发现多设备帧时间对齐异常，先复核：

```bash
chronyc tracking
chronyc sources -v
```

## 当前运行入口

接收端：

```bash
ssh fz@192.168.66.196
cd /home/fz/Desktop/wireless_video_delivery
pgrep -af "gemini_receiver|uvicorn|server.py"
```

Web：

```text
http://192.168.66.196:8080/
```

接收端状态 API：

```bash
curl http://192.168.66.196:8080/api/status
```

树莓派：

```bash
ssh berry@192.168.66.210
cd /home/berry/Desktop/wireless_video_delivery
git status -sb
pgrep -af gemini_sender
```

OrangePi：

```bash
ssh orangepi@192.168.66.206
ssh orangepi@192.168.66.133
pgrep -af gemini_sender
```

## 已知问题与注意事项

1. 本机 `rk3588-ubuntu_cam02` 当前离线，原因是配置序列号 `AY2MC3100Z8` 未找到；当前只看到 `AY2M54302ZH`。
2. 接收端有本地未提交改动，并且曾观察到落后远端多个提交；在接收端改代码前先 `git fetch` 并确认是否需要合并。
3. 两台 OrangePi 主机名都叫 `orangepi5pro`，必须用 IP、sender id、相机 serial 一起区分。
4. 本机主仓库有多份 sender config 未提交，不要直接全量提交。
5. `/tmp/wvd-rpi-gemini305` 是旧 scratch worktree，里面有大量未提交实验改动；不是正式分支。
6. Web 预览历史上出现过卡死、重复 ffmpeg、主画面不刷新等问题；当前默认主链路仍以稳定 30fps 和录制可靠性优先。
7. RGB MP4 的精确采集时间应以 `frames.csv` 为准，MP4 容器 PTS 不是下游对齐的唯一依据。

## 接手建议

1. 先看 `04_docs/23_SSH接入信息与控制设备迁移说明_2026-06-16.md`，配置新控制设备 SSH。
2. 再看本文确认当前分支与运行状态。
3. 不要先拉起新服务，先查已有进程，避免重复 sender/watchdog 或重复 receiver/web monitor。
4. 提交前总是使用：

```bash
git status -sb
git diff --stat
git diff --cached --stat
```

5. 推送时只 stage 本次明确范围内的文件，避免把本地配置试验一起推上去。
