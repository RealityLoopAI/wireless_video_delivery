# 语音拍照接收端可靠落盘

日期：2026-07-28

## 1. 需求

在 `orangepi5pro-d12a4719` 上复用已有 USB 麦克风、USB 音箱和“小环”离线语音服务：

1. 用户说出拍照命令后，由正在运行的 sender 取得目标相机下一帧 RGB。
2. 保存相机原始 MJPEG，不做 JPEG 解码和二次编码。
3. 图片经 receiver 留存到 NAS，不让 sender 直接访问 NAS。
4. receiver 本地可靠收到图片后立即返回 `captured`，语音播放“好的，已拍照”。
5. NAS 上传异步完成，不能阻塞采集、媒体发送、录制或语音确认。
6. 不改变现有 RGBD 录制目录和状态机；照片使用独立目录。

## 2. 当前链路

```text
语音服务写本地请求 JSON
  -> sender 选择下一帧完整原始 MJPEG
  -> 独立可靠 TCP 快照队列，ACK 丢失时重发
  -> receiver 独立照片队列
  -> 本地 photo.jpg + photo_ready.json + fsync + 原子任务目录
  -> UDP captured ACK
  -> 语音播放“好的，已拍照”
  -> 独立 photo uploader 校验大小和 CRC
  -> NAS 临时文件 + fsync + 原子重命名
```

快照使用媒体协议 `stream_type=4 (rgb_snapshot)`。`request_id` 放在：

```text
codec_or_compression=mjpeg;request_id=<request_id>
```

媒体包版本仍为 v2，没有修改原有固定包头。

## 3. 可靠性语义

`captured` 的精确定义：

1. receiver 已收到完整 JPEG。
2. JPEG SOI/EOI、大小上限通过检查。
3. `photo.jpg` 已写入并 `fsync`。
4. 带 CRC32、帧时间戳和 NAS 相对路径的 `photo_ready.json` 已写入并 `fsync`。
5. 临时任务目录已原子重命名，staging 根目录已 `fsync`。

`captured` 不表示 NAS 已上传完成。这样 NAS 断开时照片仍有 receiver 本地可靠副本，语音不需要等待 SMB 往返。

sender 在 30 秒内保留待确认请求，每秒重发一次未确认快照。receiver 对同一 `request_id` 幂等处理，并连续发送三次 ACK。队列满、格式错误或 staging 不可用时返回明确失败，不影响 RGBD 主链路。

## 4. 目录

receiver 本地 staging：

```text
/home/fz/recording_staging/.gwv3_photo_queue/<request_id>/
  photo.jpg
  photo_ready.json
```

最终 NAS：

```text
/home/fz/Desktop/nas/voice_photos/<sender_id>_<camera_id>/YYYY-MM-DD/HH-MM-SS/YYYYMMDD_HHMMSS.jpg
```

时间取自被选中 RGB 帧的 `system_timestamp_us`。同一相机同一秒内重名时使用 `_001`、`_002`。最终 NAS 只保留 JPG，不写旁路 JSON。

## 5. 隔离边界

1. `rgb_snapshot` 不进入 Web 预览。
2. `rgb_snapshot` 不进入 `frames.csv`、MP4、MKV 或录制切片。
3. sender 使用独立 TCP 连接和有界可靠队列，不占 RGB/Depth 最新帧队列。
4. receiver 的 staging 写盘在独立 worker 中，不阻塞 media client thread。
5. `gwv3-photo-uploader.service` 与 `gwv3-recording-uploader.service` 分离。
6. uploader 检测 NAS 挂载；未挂载时不向空挂载点误写。

## 6. 原始画面约束

保存内容是相机提供的原始 MJPEG 字节。若 sender 的实时视频通过软件做了 180 度旋转，照片不会继承该软件旋转。这是“原始 MJPEG、不二次编码”的直接结果；如后续必须保存旋转后的照片，需要单独定义无损元数据或允许解码后重编码，不能混淆两种语义。

## 7. 配置与服务

receiver 配置：

```json
{
  "photo_capture": {
    "enabled": true,
    "staging_root": "/home/fz/recording_staging/.gwv3_photo_queue",
    "nas_subdirectory": "voice_photos",
    "max_jpeg_mb": 8,
    "queue_max_items": 128,
    "upload_interval_ms": 250
  }
}
```

服务：

```text
gwv3-gemini-receiver.service
gwv3-photo-uploader.service
xiaohuan-wake.service
```

## 8. 测试

本机自动测试：

1. sender C++ 编译和 `sender_config_validation` 通过。
2. receiver C++ 编译及全部 6 项 CTest 通过。
3. `voice_photo_pipeline_integration` 启动真实 receiver，模拟 heartbeat 和 v2 MJPEG 快照包，验证 `captured` ACK、原始字节、时间路径和状态计数。
4. `photo_uploader_fault_injection` 验证正常发布、内容幂等、重名 `_001`、CRC 错误保留、目录穿越拒绝和 NAS 未挂载等待。
5. Python `py_compile` 与 systemd 安装脚本 `bash -n` 通过。

实机部署和真实相机验证结果记录在同日 `08_reports` 专项归档。
