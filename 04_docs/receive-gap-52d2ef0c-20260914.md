# 52d2ef0c 接收停顿专项排查与诊断修复

日期：2026-09-14。对象：`lubancat-52d2ef0c_cam01/2026-09-11/174304`。

## 结论及修复范围

历史两次 9–10 秒停顿属实，主要滞留区间在编码完成之后。此次修复发送耗时统计漏算重试、缺少接收处理分段计时和发送日志历史过短的问题，并增加 TCP 诊断。

**没有证据可以宣称已消除历史停顿的根因。** 当前仍需在再现时区分发送应用排队、内核 TCP/无线重传和接收处理背压。这是一轮诊断缺陷修复，不把增加日志冒充实时延迟已经根治。

没有改相机参数、TCP 协议、时间戳/CSV 语义或录制队列上限；没有通过丢帧、重连清空积压来缩短接收时间。

## 原始证据核对

本次读取 NAS 原 `frames.csv` 的 SHA-256 与附件一致：

```text
8989018797e4112c00de992b3c5779f4245a15e018ff3f7d49efea6a47c6c11d
```

保持 CSV 原行序，不按时间重新排序：

| 指标 | 第一次，3062→3066 行 | 第二次，4496→4500 行 |
| --- | ---: | ---: |
| RGB frame_id | 633860→633861 | 634577→634578 |
| 视频帧索引 | 1529→1530 | 2246→2247 |
| 接收间隔 | 9.450768 s | 10.254619 s |
| global 时间间隔 | 33.282 ms | 33.034 ms |
| 后一帧编码耗时 | 39.597 ms | 33.616 ms |
| 后一帧准备入队至接收，原始主机时间差 | 9.415089 s | 10.246583 s |
| 后一帧接收入口至 CSV 行 local_time | 176 us | 212 us |

跨主机时间差包含时钟偏移，约百微秒量级的偏移不足以解释十秒；CSV 中派生的 queued_to_receiver 列与直接相减略有差别，未将两者混用。local_time 也不是 fsync 完成或 NAS 持久化时间。

这些相邻帧不是“采集少了十秒”的证据，也没有逐帧解码画面来断言回放冻结。附件只覆盖两组 RGB，相机整个片段的完整性需要另做全量检查。

## 接收日志提供的补充

- 17:44:00：设备超时，录制进入 waiting for recovery。
- 17:44:05：Depth 接收间隔 9.430108 s，RGB 9.450768 s，随后恢复。
- 17:44:24：再次进入等待。
- 17:44:26：Depth 恢复，间隔 6.529768 s。
- 17:44:30：RGB 恢复，间隔 10.254619 s。
- 两路 TCP session 分别为 RGB 13、Depth 14。已观察到两个流受影响，不能归为仅一帧 RGB 编码异常。

当时已启用 120 秒媒体恢复策略，没有因为上述 5 秒停顿判定而提前关闭该片段。此前“提前收尾”修复生效不等于消除了网络/处理停顿。

本次 sender.log.1 最早已到 9 月 14 日 07:25，没有保留 9 月 11 日此时段；普通和 sudo 查询 NetworkManager 同期日志均无记录。这是证据缺失，不是证明没有发生网络故障。

9 月 12 日凌晨 NAS ENOSPC 是另一事件；没有用 18:30 才开始的 NAS 采样解释本案 17:44。

## 代码确认的时间边界

- Sender 在编码完成、准备发布 MediaPacketJob 前记录 packet_queued 时间。它不代表 TCP 已发送或对端已确认。
- Receiver 在 read_media_packet_into 完整读取和解析后，进入 handle_media_packet 时记录 receiver_receive_timestamp_us。
- 此字段不是内核网卡到包时间。上一包处理阻塞会延后读取下一包，因此时间差不能全算作无线时延。
- 录制队列有界且容量满时会等待，这是数据保留策略；不能未经定位改成丢弃数据，只求预览数字好看。

## 本次代码修改

1. MediaPacketJob 保留单调时钟创建时间和首次发送尝试时间，重试时一起移动，不重置。
2. 原 send_ms 仅统计最后一次 send_media 调用；现改为从首次尝试到成功/失败的完整时间，包括背压重试及其间隔。
3. 延迟超过 250 ms 时，限频记录 camera、stream、frame、queue_ms、send_total_ms 和 sent。
4. 同时记录 TCP 发送队列字节、RTT、RTO、未确认包和累计重传计数。累计重传必须比较差值，不能当作本帧重传量。
5. Receiver 拆出 read_wait_ms、handle_ms，并记录处理结束时 socket_unread_bytes。任一耗时超过 250 ms 时限频输出，关联 frame/session。
6. sender.log 原来只保留 .1，改为最多 .1–.8，连同活动日志共最多 9 份。按配置的每文件容量轮转，不承诺固定保存天数。

250 ms 是诊断触发阈值，不是已经商定的业务验收阈值。诊断限频每连接/发送工作循环约一秒，不是每一异常帧必然都有日志；正常发送不持续记录 TCP 历史。这些限制应在解读结果时保留。

## 下次出现时如何定位

| 组合证据 | 优先排查，不是单指标定罪 |
| --- | --- |
| queue_ms 大 | Sender 出队不及时、前序包发送阻塞或调度延迟 |
| send_total_ms 大、TCP 队列高、重传差值增加 | 网络丢包/重传、拥塞及接收窗口；必要时补充抓包 |
| Receiver read_wait_ms 大，handle_ms 小 | 此连接长时间没完整读到包；仍可能是源端没有交付、网络或线程调度 |
| Receiver handle_ms 大，socket_unread_bytes 高 | 接收侧处理使后续包留在 socket，包括录制队列背压/锁/预览路径 |
| 两者都不大但跨帧异常 | 需要核查日志限频、系统调度和内核到包时刻，不能强行套上述结论 |

TCP send 成功只说明数据被本机内核接受，不保证 receiver 已到达；诊断不会改变这一事实。

## 测试与部署

本机 ARM 编译 Receiver 通过。针对真实 TCP socket 的隔离故障注入：接收程序暂停读取 10 秒，发送发生 90 次 pending 重试，最长单次 send 调用约 100.215 ms，8,388,796 字节在同一连接恢复后逐字节一致。

这验证保留数据和新 TCP 诊断字段，没有“把十秒停顿优化为零”。不是对生产 AP 注入故障，也没有填满生产 NAS。

接收端媒体恢复测试增加 stage 日志字段断言。x86 接收端完整 CTest 40/40 通过，耗时 100.13 秒。52d2 使用原 SDK 2.8.6 编译，24 项 CTest 中 12 项通过、12 项因缺少软件编码插件跳过、0 失败。构建保留了系统 libmali 的链接警告，不将跳过项计为通过。

源码提交：`9c8bc21`。已在录制及收尾空闲后部署接收端和本案 52d2ef0c；API 核实两者 build_commit 均为 9c8bc21c5cfa。其余 sender 未修改，网络与相机配置也未改。

二进制 SHA-256：

- Receiver：`746e023e971e99738073e7dc3484539e5de25c2aacab558e0e3160a632f6a653`。
- 52d2 Sender：`90cbcdc1baa14a1006095cbfe59ba8b5b899b7f6f09fa8d6c6fb71137708caa9`。

旧二进制分别备份在各自生产 `12_build/bin` 下：`gemini_receiver.before-gap-9c8bc21`、`gemini_sender.before-gap-9c8bc21`。回退前需确认没有录制和收尾，再停止相应服务、原子恢复旧二进制并启动；配置无需回退。

## 上线后实录

仅对 52d2 做约 35 秒控制时长的实录，session 为 `1789356801300096`，实际录制窗口约 34.001 秒。NAS 目录：

```text
lubancat-52d2ef0c_cam01/2026-09-14/113322
```

- RGB 1020 帧，Depth 1020 帧；两路时钟无效行均为 0。
- RGB 最大接收间隔 187.973 ms；Depth 最大接收间隔 178.182 ms。
- 停止后约 14.116 秒，发布目录出现且上传/收尾队列排空。
- 两个媒体文件各 1020 个 packet，PTS/DTS 无非递增；完整解码复测均无报错。
- 初次默认 null 输出测试出现输出端 non-monotonic DTS 告警。原输入 packet 的 PTS/DTS 严格递增；改用保留输入时间基的 `-vsync 0 -enc_time_base -1` 解码，两路均通过。没有改录像来消除告警，初次测试没有被记成通过。
- 实录结束，当前状态 idle，待上传片段 0，五路仍在线。

该短测没有复现历史九秒级停顿，不足以证明长期无线运行无停顿。历史问题保持“根因待进一步定位”，不是已关闭。下次事件须结合新增阶段日志，必要时在授权窗口做受控链路故障与内核抓包。

## 继续排查：11:41 至 11:55 实时证据

上述“idle”仅指短测结束时。之后用户开始新的五路录制，本轮保持录制，不切 AP、不重启、不修改曝光、队列或媒体配置。

### 已复现 TCP 交付停顿，但未复现历史十秒事件

11:52:35 至 11:52:37，52d2 的阶段日志给出以下关联证据：

| 指标 | RGB | Depth |
| --- | ---: | ---: |
| 保留重试的 frame_id | 37471 | 37473 |
| 首次出队等待 queue_ms | 0.085467 | 0.075693 |
| 11:52:36 累计发送等待 ms | 1423.249429 | 1423.409565 |
| TCP Send-Q bytes | 122512 | 158027 |
| 11:52:35→36 累计重传计数 | 2375→2376 | 2726→2727 |
| 恢复后另一帧 queue_ms | 1837.650653（37488） | 2284.252191（37477） |
| 接收端接收间隔 ms | 2493.131 | 2510.013 |
| 接收端 read_wait_ms | 2492 | 2492 |
| 接收端 handle_ms | 0 | 12 |

预览也在同一时间等待约 2492 ms，预览连接因部分写超时重建；正式 RGB/Depth 则保留 pending packet 重试。TCP send 成功不等于远端已确认，累计重传计数也不是这一帧的重传次数。

**本次机制已经定位到：TCP 交付受阻，正式媒体保留重试，随后应用发送队列积压，恢复后追赶发送。** 不能据此断言每个采集帧都最终写入，也不能反推 9 月 11 日一定是同一物理故障。

### 两端限时抓包交叉检查

两端分别对 wlan0 / ens3 抓取对方 TCP 50010，35 秒后自动结束，snaplen=96；未采集完整媒体内容，但包头后可能含少量载荷前缀。原始抓包不放入 Git。

- Sender 捕获 50415 包，Receiver 捕获 24175 包；两端报告内核抓包丢弃均为 0。
- 共同时间区间 33.434259 秒，起始 Unix time 为 1789357576.760098。
- 按源端口、目的端口和 TCP sequence 起点匹配首个数据包，匹配 14930 个起点。
- 匹配子集的接收抓包时刻减发送抓包时刻：P95 47.291 ms，最大 1220.053 ms；100 个起点超过 250 ms。
- 共同区间未见接收端通告零窗口。
- Sender 重复的完整 sequence/length 区间计数，按源端口：44740=81、53922=17、53930=38、44734=56。这是候选重传区间，不单独作为精确丢包率。

发送与接收分段长度不同，存在分段/聚合影响，不能用包数相减推断丢包。跨主机时间差没有做硬件时间戳校正，也不等于纯无线空口时延；发送抓包点仍在实际无线发送之前，接收虚拟机调度也可能影响观测。11:52 的 chrony 均为 Normal，52d2 以接收端为参考，报告系统偏差约 39 us；接收端约 45 us。这是后续时钟采样，不是逐包时钟误差上界。

这些证据支持停顿发生在接收应用处理之前；仍需区分 Sender 驱动/无线/AP 转发和 Receiver 虚拟机调度。没有把“未见零窗口”等同于排除所有接收端问题。

### 接收队列、采集与设备状态

- 约一分钟、每 10 秒一次的七次状态采样，52d2 录制队列字节和背压等待计数均为 0；其中一次 Depth 到达延迟约 1462 ms。离散采样不能排除采样间隙的瞬时排队。
- 11:55 前后的五路状态均在线、录制中，写盘错误与背压等待计数均为 0，时钟同步均有效。52d2 输入约 30 fps；fe0f7222 有一次 Depth 输入约 28 fps，不能据此宣称全系统持续满帧。
- 另见 11:52:38，52d2 frame_id=37586 源 MJPEG 的 SOI/EOI 完整性检查失败被丢弃。它与 TCP 停顿是两类问题，尚未定位是相机、USB、SDK 缓冲还是上游数据异常。
- 52d2 连到 `666666`，BSSID `14:20:04:7f:74:19`，5180 MHz，RSSI 约 -32 dBm，协商 TX 434 Mbps。信号和协商速率不等于持续可用吞吐。
- Wi-Fi 使用 rtl8821cu，USB2 上挂 Wi-Fi/BT 与音频；相机为独立 USB3 5000M。不能仅凭 USB2 推断带宽不足。
- Wi-Fi power_save=off，驱动 power_mgnt/ips_mode/enusbss/low_power 均为 0，USB power/control=on，已经关闭省电，不能再把重复关闭省电冒充修复。
- Receiver 是五 vCPU 虚拟机，采样 load 约 6.8；当前包 handle 很短，但不能因此完全排除宿主机/虚拟机调度。

### 后续修复与验收边界

本轮没有再修改生产代码或配置，只补充取证和报告。之前的诊断修复有效，**实际 TCP 停顿仍存在，问题不能关闭**。

录制结束后，建议按单变量顺序进行：

1. 保持档位和码率，在当前 AP 与另一 AP 分别采样，比较同长度窗口的重传差值、最长 read_wait、帧覆盖率，不能只看平均 Mbps。
2. 两端同时采集 CPU 调度/虚拟机 steal、驱动统计和低负载链路探测，区分 Wi-Fi 驱动/AP 与 Receiver 调度；先定位再决定驱动或网络调整。
3. 源 MJPEG 错帧单独统计 frame_id 与 USB/SDK 日志，禁止放宽校验把坏帧送入正式录像。
4. 修改后重新做长录制，从原序 CSV 核查采集间隔、接收间隔、时钟有效性及首尾覆盖。少量缺帧与最大可接受到达延迟需分别验收，不能以 ready 或零写盘错误代替内容完整性。

不在当前用户录制中用重连清队列、降低档位或切网来制造“延迟已经消失”的结果。

## 14:00 后进一步下钻：无线接入与 USB 发送通路

### 分段探测，而非继续只看 TCP

52d2 同时向网关、Receiver、NAS 各发每秒一个 ICMP 请求，60 包均收到响应：

| 目的端 | 平均 RTT ms | 最大 RTT ms |
| --- | ---: | ---: |
| 网关 192.168.1.1 | 13.572 | 158.932 |
| Receiver 192.168.1.196 | 12.936 | 158.215 |
| NAS 192.168.1.89 | 12.784 | 164.337 |

seq=43、45 等相同采样秒，三个目的端同时抖动。这支持优先检查共同接入路径，不支持将这些 RTT 都归因于 Receiver 应用。第一次命令使用目标机不支持的 ping -D，未得到探测数据；以上为检查帮助后重新运行的有效结果。

反向在 Receiver 同时探测三端，各 75 包全部响应：

| 目的端 | 平均 RTT ms | 最大 RTT ms |
| --- | ---: | ---: |
| 52d2 | 33.957 | 1168.917 |
| 网关 | 0.781 | 2.630 |
| NAS | 0.059 | 0.193 |

52d2 的长 RTT 发生在 Unix time 1789366052.964369 附近；同轮有线到网关和 NAS 未见相应秒级抖动。缩小到 52d2 接入分支，不是宣称 AP 与驱动已经二选一定位完毕。

### 排除项的实际边界

- 60 秒、5 秒间隔的 13 次 qdisc/softnet 采样：pfifo dropped 始终为 25730，没有新增；softnet 的 drop/time_squeeze 均为 0。不能把累计 25730 当成这次丢包。
- Sender vmstat 短测 CPU idle 约 77–79%，无 swap；Receiver 短测 idle 63–70%、steal 0–1%。短采样不代表所有历史事件均无调度问题。
- 14:06:14–14:07:44 使用独立 ftrace instance，只启用 cfg80211 rdev_scan / scan_done，零事件，结束后已关闭并删除 instance。它排除了该窗口通过这两个接口的扫描，不排除固件内部行为，也没有覆盖反向探测中稍后的 1.169 秒峰值。
- 原厂派生驱动编译配置 CONFIG_PROC_DEBUG=n，所以没有 /proc/net 下专用调试目录。缺统计不是“统计为零”；没有为了增加调试接口在线卸载唯一无线驱动。

### USB 层观测到了发送请求慢完成

使用内核 [usbmon](https://docs.kernel.org/usb/usbmon.html)，只保留 Wi-Fi 设备 `3:004` 的 bulk 请求元数据。按同一 URB 标识匹配提交 S 和完成 C，记录时间差，不保存 USB 载荷。独立临时工具见归档中的 `usb_latency_probe.py`。

采样 Unix time 1789366065.6765666–1789366125.6866596，约 60 秒：

| 端点 | 匹配请求数 | P95 完成耗时 | 最大完成耗时 | 超过 100ms |
| --- | ---: | ---: | ---: | ---: |
| Bulk OUT 8 | 26798 | 0.830 ms | 430.422 ms | 36 |
| Bulk OUT 6 | 39 | 0.159 ms | 0.258 ms | 0 |
| Bulk IN 4 | 4346 | 174.056 ms | 489.870 ms | 2567 |

匹配到的 Wi-Fi bulk 完成/提交错误状态计数为空，结束时有 8 个未完成请求，不能将它们认定丢失。没有读取 usbmon 丢弃统计，本测不能保证完整捕获每个请求。

**有价值的是 Bulk OUT 的长尾：数据已经作为 USB 请求提交，仍发生百毫秒级慢完成。** 同期 14:08:32–36 Receiver 的 RGB/Depth/预览 read_wait 达 349–510 ms，handle 为 0–17 ms。但此工具第一轮慢事件清单前 40 项被 IN 请求占满，不能逐次声称某个 OUT 请求就对应某一帧。

Bulk IN 请求可以预先排队等待网卡收包，不能用其等待时间直接宣称“USB 收包耗时 174 ms”。Bulk OUT 慢完成也不等于 USB 线坏：可能是网卡/固件因空口重试而回压，也可能是 USB 主控或调度延迟。没有看到错误码或复位日志，不支持直接要求换线/改 PCB。

当前底层定位比上午更进一步：**无线接入路径存在独立于媒体应用的长尾，而且无线网卡 USB 发送请求本身有慢完成。** 下一次受控比较应同时保留 OUT 慢请求时刻、驱动发送完成统计和 AP 每站重试/空口占用；在固定档位下只改变 AP 或驱动一个变量，才能将网卡回压与主控调度分开。没有把这一观察冒充具体芯片/寄存器根因。

### 另一个独立问题：录制保护停止的错误分类

14:00 查询时系统已 faulted，故障时间为 1789364946537072（13:49:06），五路报 below configured reserve。本轮没有发停止录制请求。后续查询本地可用约 222 GiB，NAS 可用约 2.06 TiB，待上传为 0，不能据此倒推 13:49 时磁盘一定够用或一定满了。

代码 `storage_space_meets_limits` 将 NAS 容量快照过期、无效、不 ready、路径不匹配和容量真正不足都返回 false，调用方统一写为容量不足。这是已确认的错误分类/可观测性缺陷；本轮未改动该行为、降低保护阈值或自动恢复录制。应独立修复错误原因细分并保留触发时的快照证据，避免错误地将安全保护全部解释成 NAS 满盘。
